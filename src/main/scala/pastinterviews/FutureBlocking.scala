/**
 *
 * Three threads call a method
 * unless first is not finished other should wait for it to finish execution
 *
 * means, ist call1 should finish then second then third
 *
 * inside put Thread.sleep
 */